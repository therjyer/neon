const sayday = (prefix, pushname) => {
	return `**Bom dia!**`
}

const sayafter = (prefix, pushname) => {
	return `**Boa tarde!**`
}

const saynight = (prefix, pushname) => {
	return `**Boa noite!**`
}

module.exports = {
	sayday,
	sayafter,
	saynight
}